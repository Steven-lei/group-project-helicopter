services:
  mongo:
    image: mongo:7
    container_name: moodpal-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: moodpal
    volumes:
      - moodpal_mongo_data:/data/db

volumes:
  moodpal_mongo_data:
