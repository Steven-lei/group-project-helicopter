# Backend of the group project

## Steps

1. copy .env.example to .env
2. configure DATABASE_URL using the correct database parameters
3. configure the cert file(an Azure ca file is included by default)
4. run npm test-db to test connection
